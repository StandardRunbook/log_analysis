# Metadata Service Configuration (gRPC)
METADATA_GRPC_ENDPOINT=http://localhost:50051

# ClickHouse Configuration
CLICKHOUSE_URL=http://localhost:8123
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=your-password-here
CLICKHOUSE_DATABASE=default

# LLM Configuration (using Rig)
# Supported providers: openai, anthropic, cohere, ollama
LLM_PROVIDER=openai
LLM_API_KEY=adfasdfadsf
# Model will be auto-detected based on provider if not specified
# Examples: gpt-4, gpt-3.5-turbo, claude-3-sonnet-20240229, command
LLM_MODEL=gpt-5

# Ollama Configuration (optional - for local LLM)
# If using Ollama, set LLM_PROVIDER=ollama
# OLLAMA_ENDPOINT=http://localhost:11434
# OLLAMA_MODEL=llama2

# Testing Configuration (for benchmarks)
# Set these environment variables when running tests with Ollama:
# export OLLAMA_ENDPOINT=http://localhost:11434
# export OLLAMA_MODEL=llama2

# Note: The REST API server runs on http://127.0.0.1:3001 (hardcoded)